<html>
<h1 style="font-family:Helvetica; font-size:25px;">EYP Conversation Progress</h1>
<h2 style="font-family:Helvetica; font-size:18px;">People covered: {{percentCovered.value}}% <%- JSON.stringify(twodArray)%></h2>

<style type="text/css">
.eyp  {border-collapse:collapse;border-spacing:0;}
.eyp td{font-family:Helvetica, sans-serif;font-size:11px;padding:10px 5px;border-style:solid;border-width:1px;overflow:hidden;word-break:normal;}
.eyp th{font-family:Helvetica, sans-serif;font-size:11px;font-weight:normal;padding:10px 5px;border-style:solid;border-width:1px;overflow:hidden;word-break:normal;}
.eyp .eyp-yw4l{vertical-align:top}
</style>
<script type="text/javascript" src="ejs.js"></script>

	<table class="eyp" >
	<% for(row=0; row<twodArray.length; row++){ %> 
		
		<% for(record=0; record<row.length; record++){ %>
			<% if (row==0){ %>
				<th class="eyp-yw4l" > <%=record.value%></th>
			<% } else {%>
					<tr>
					<% if (record.flagValue == 1){ %>
						<td class="eyp-yw4l" bgcolor="green">
					<%} else if (record.flagValue == 0) {%>
						<td class="eyp-yw4l" bgcolor="red">
					<%} else {%>
						<td>
					<%}%>
					<%=record.value%></td>
					</tr>
				<% } %>
		<% } %>
	<% } %>
	</table>
</html>